// const Database = require("../service/database/index");
// const pool = require("../service/database/index");
// import { Request, Response, NextFunction } from "express";

// export const withDatabase = async (controller: Function) => {
//   return async (req: Request, res: Response, next: NextFunction) => {
//     try{
//         await controller(req, res,next,);
//     }
//   };
// };
